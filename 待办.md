# 待办（hezuriji）

更新：2026-08-03

## 下一单候选（按优先级）

1. **短信系统**：每日限 3 条 0:00 重置；快捷短语+自定义；短信页只显示真实电波（你发的/TA 回的/已读不回），内心独白只进城市日志；规则模板先行，`BrainHook` 挂点不变。（对标 citylife 已验证版本）
2. **导演层**：200 分钟无戏 → 30% 概率下雨 90–140 分钟（治"跑平了"，从 v0.5 提前）。
3. **动态流体验**：只在读者位于底部时自动跟随，否则出"↓ 回到最新"按钮；渲染纪律=建一次原地改数。
4. **手柄空间寻焦**：替换列表式焦点（navScore 纯函数，可入验证门）。
5. **钱系统二期**：外卖链路（顾客→厨师→骑手串线）、城市日历（周五发薪/周日街市/1 号交租已有雏形对齐口径）。
6. **离线快报**："你不在的时候"五幕+明信片（需存档 localStorage + 补跑）。

## 已登记（暂不动）

- LLM 三级兜底链（直连→中转→模板）：等短信系统上线后接，BrainHook 为唯一替换点。
- 睡前日记（打包调用/锁时间戳/占位回填）：接 AI 时照 citylife 口径落地。
- 同屋闲聊不对称记忆：v0.3 关系系统一起做。
- 本地小模型（Ollama 4B）端侧方案：等公开版再议。
- 部署（EdgeOne Pages / Vercel）：等一周沉浸测试过后再立项。
- 手柄真机实测、iOS 真机实测：等决策者上手反馈。

## 等决策

- 仓库是否公开（当前私密）。
- 部署平台与自定义域名（需决策者名下资源）。
