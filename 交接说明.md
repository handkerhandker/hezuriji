# 交接说明（hezuriji · 合租日记）

更新：2026-08-03 · 基线：v0.1

## 项目一句话

《合租日记》：AI 小人生活模拟。5 个小人在合租公寓+街区自主生活（吃饭/上班/闲聊/交租/求职），玩家旁观、刷动态流、翻日报；短信系统（玩家递纸条）待做。

## 技术形态

- React 19 + TypeScript + Vite + Tailwind（webapp-building 脚手架），纯前端无服务器，`npm run dev` 即玩。
- 引擎与 UI 严格分层：`src/engine/` 纯 TS 零 DOM 依赖，可 headless 裸跑（node/tsx 直接驱动）。

## 固定验证门（CI 强制执行，.github/workflows/gate.yml）

```
npm run build
npx tsx scripts/sim-check.mts
npx tsx scripts/check-walk.mts
```

## 代码结构

- `src/engine/`
  - `types.ts`：世界/小人/活动/事件类型。硬事实（钱、账）一律结构化，不走模型。
  - `data.ts`：5 张人物卡（特质/怪癖/心愿/担忧/岗位）、房租 450/30 天、LLM 日预算 40。
  - `actions.ts`：行动空间（物件 affordance）；特质硬约束过滤（节俭看不到贵价选项）；**路程时间表 TRAVEL_HOURS**（跨街区 0.25–0.55h）。
  - `decide.ts`：双系统决策。效用打分（系统1）+ 显著性门控（系统2=LLM 挂点 `BrainHook`，未接入时规则兜底）。
  - `sim.ts`：Sim 类。10 分钟一步（stepTick）；移动占真路程时间（fromLoc/toLoc 冻结）；日结工资按出勤；交租日（每 30 天）；日终反思记账；指标落盘。
  - `metrics.ts`：动作熵/基尼（offset 版，允许负债）/社交密度/戏剧温度。
  - `report.ts`：城市日报 + 状态叙事化（"饿得有点狠了"，不写数值）。
- `src/ui/`
  - `PixelScene.tsx`：像素四街区画布，代码绘制零素材；JS 实测父容器等比缩放；点小人选中。
  - `sceneMap.ts`：站位推导 + 走路路径（屋里→门口→路口→门口→点位，四段无缝）。
  - 其余面板：场景/住户卡（心愿担忧外显）/动态流/指标/日报弹窗/顶栏。
- `src/hooks/`：useSim（主循环）、useLastInput（输入设备侦测）、useGamepad（手柄轮询）。
- `scripts/`：sim-check.mts、check-walk.mts、debug-alang.mts（排障工具）。

## 关键设计口径（冻结对象，宪法第 7 条）

- 时间：10 分钟一步；1x = 现实 1 秒 ≈ 游戏 40 分钟。
- 移动：按路程时间表占时，画面按进度走 L 形路线，禁止瞬移。
- 性格：结构化特质 = 行动空间物理约束，不是形容词。
- 记忆/关系：结构化事实账目（谁请了谁、欠多少），无好感度数字。
- LLM：只在有戏时刻唤醒（显著性门控），每日预算池 40，池空退回规则；默认零成本。

## 踩坑备忘（改码前必读）

- 活动结算会吞当前小时：结算后必须当步立刻再决策（stepAgent 循环），否则出勤/日程全歪。
- 活动效果必须启动时冻结快照：醒来时再查"合法集"会丢掉（睡觉曾拿不到精力）。
- 出勤按实际干活的那一小时计（untilHour-1），别按结算时刻。
- Edge 无头截图最小视口 492px 且 sw==cw，是工具怪癖不是页面溢出；判溢出要用空白页对照。
- 基尼系数遇负债（负值）会爆表，必须先 offset 平移再算。
- 缺零成本兜底食物会出现"没钱→永久挨饿"死循环；`scrap_fridge` 是安全阀。

## 当前状态与下一步

见 `待办.md`。LLM 通道（BrainHook 三级兜底链，对标 citylife）为唯一替换点，接 AI 不动游戏本体。
